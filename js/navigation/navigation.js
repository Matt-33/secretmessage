export function redirectToMessage(url) {
    // Redirection vers l'URL 
    window.location.href = url;
}

export function redirectToHome() {
    // Redirection vers la page d'accueil
    window.location.href = 'index.html';
}